# Postgres Notes

- Install packages: postgresql and postgresql-contrib
- Create role and database for the app
- Ensure pg_hba.conf allows local connections
- Expose to app via DATABASE_URL

Prefer least-privileged role and strong passwords.
